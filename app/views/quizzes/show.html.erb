<div class="container" id="quiz">

<h1>Today's Quiz <small>(23 Questions)</small></h1>	
	<div class="row">
		<div class="col-md-2" id="quiz_nav">

			<% word_types.each do |wt| %>
	    	<%= link_to wt.name.capitalize.pluralize, progress_quiz_path(id: "1", word_type_id: wt.id), remote: true, class: 'btn btn-success btn-block' %>
	    <% end %>
		</div>

		<div id="quiz_container" class="col-md-10"></div>
	</div>
</div>
