<nav class="navbar navbar-default">
	<div class="container-fluid">
		<div class="navbar-header">
			<%= link_to "Lingo", root_path, class: 'navbar-brand' %>
		</div>
		<div class="navbar-collapse collapse">
			<ul class="nav navbar-nav navbar-left">
			 <li>
			 	<%= link_to "All Languages", languages_path %>
			 </li>
			 <% current_user.languages.each do |lang| %>
			 <li>
			 	<%= link_to "#{lang.name}", language_path(lang) %>
			 </li>
			 <% end %>

			 <li class="dropdown">
			  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Vocabulary <span class="caret"></span></a>
			  <ul class="dropdown-menu">
			    <% word_types.each do |wt| %>
			    	<li><%= link_to wt.name.capitalize.pluralize, word_type_cards_path(wt) %></li>
			    <% end %>
			    <li role="separator" class="divider"></li>
			    <li><%= link_to "Cards", cards_path(index: "42") %></li>
			  </ul>
			</li>
			<li>
				<%= link_to "My Progress", '#' %>
			</li>
			</ul>
			<ul class="nav navbar-nav navbar-right">
				 <li>
				 	<%= link_to current_user.email, users_show_path %>
				 </li>
				<li>
					<%= link_to 'Logout', destroy_user_session_path, method: :delete %>
				</li>
			</ul>
		</div>
	</div>
</nav>